language: cpp

compiler:
    - gcc
    - clang

before_script:
    - sudo apt-get update -qq
    - sudo apt-get install libboost-thread-dev libboost-system-dev
    - mkdir build
    - cd build
    - cmake ..

script: make